<!DOCTYPE html>
<html>
  <head>
    <title>Cayana</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body>

      <nav>

        <div class="item_nav">
        <a href="/">
              <%= image_tag ("logo.png") %>
              <strong>Cayana</strong>
        </a>
        </div>

        <div class="item_nav">
        <% if user_signed_in? %>
        <ul>
          <li><a href="#">Search</a></li>
          <li> <%= link_to "Tsip!", new_article_path, role: "button" %> </li>
        </ul>
        <% end %>
        </div>
        <ul>
          <li>

            <div class="div_profile item_nav">
            <% if user_signed_in? %>
            <! -- Logged in as <%= current_user.email %>. -->


            <% if current_user.avatar.present? %>
            <%= link_to image_tag(current_user.avatar, width: "80px", :style => "border-radius: 100px"), edit_user_registration_path, :class => 'navbar-link' %>

            <% else %>
            <%= link_to image_tag("default_avatar_small.jpg", width: "80px", :style => "border-radius: 100px"), edit_user_registration_path, :class => 'navbar-link' %>

            <% end %>

            <%= button_to image_tag("right.png",  width: "30px"), destroy_user_session_path, method: :delete, :class => 'navbar-link' %>

            <% end %>
            </div>


          </li>
        </ul>

      </nav>
    <main>
      <%= yield %>
      <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
      <% end %>
    </main>
  </body>
</html>
